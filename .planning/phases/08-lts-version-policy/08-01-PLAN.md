---
phase: 08-lts-version-policy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true

must_haves:
  truths:
    - "Future maintainers understand version selection philosophy"
    - "Policy is discoverable in project documentation"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Version Policy section"
      contains: "LTS > stable > latest"
  key_links: []
---

<objective>
Document the LTS version policy in CLAUDE.md.

Purpose: Codify the version selection philosophy so future maintainers understand why tools use LTS/stable versions.
Output: New "Version Policy" section in CLAUDE.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-lts-version-policy/08-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Version Policy section to CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
Add a "Version Policy" section to CLAUDE.md after the "Code Style" section.

Content to add:

```markdown
## Version Policy

Tools use the most stable available version:

1. **LTS preferred** - Use LTS when available (e.g., `nvm install --lts`)
2. **Stable fallback** - Use stable channel when no LTS (e.g., Rust, Neovim)
3. **Latest acceptable** - Use latest when no LTS/stable distinction (e.g., Pulumi)

Package managers (Homebrew, apt, pacman) provide stable versions by default.

### Current Implementation

| Tool | Version Strategy | Method |
|------|-----------------|--------|
| Node.js | LTS | `nvm install --lts`, `nvm alias default lts/*` |
| Rust | Stable | rustup defaults to stable toolchain |
| Neovim | Latest stable | GitHub releases (NOT apt - apt versions are outdated) |
| Go | Current stable | Pinned version (Go has no LTS) |
| All Homebrew | Stable | Formulae provide stable versions |
| All apt/pacman | Stable | Repos provide stable versions |
```

Place this section between "Code Style" and "Claude Code Configuration" sections.
  </action>
  <verify>grep -q "Version Policy" CLAUDE.md && grep -q "LTS preferred" CLAUDE.md</verify>
  <done>CLAUDE.md contains Version Policy section with LTS > stable > latest hierarchy and implementation table</done>
</task>

</tasks>

<verification>
- [ ] `grep "Version Policy" CLAUDE.md` returns match
- [ ] `grep "LTS preferred" CLAUDE.md` returns match
- [ ] `grep "Stable fallback" CLAUDE.md` returns match
- [ ] Section appears after "Code Style" and before "Claude Code Configuration"
</verification>

<success_criteria>
1. CLAUDE.md contains new "Version Policy" section
2. Policy clearly states: LTS > stable > latest priority
3. Current implementation table documents actual tool strategies
4. Section is logically placed in document structure
</success_criteria>

<output>
After completion, create `.planning/phases/08-lts-version-policy/08-01-SUMMARY.md`
</output>
