all:
  vars:
    theme_color: tokyonight
    theme_font: dejavu
    theme_style: angle
  children:
    macs:
      hosts:
        macbookair:
          ansible_host: macbookair.home.lan
          ansible_user: matt
          ansible_ssh_timeout: 60
          ansible_python_interpreter: /opt/homebrew/bin/python3
          theme_color: dracula
          theme_font: sourcecodepro
          theme_style: round
        macmini:
          ansible_host: macmini.home.lan
          ansible_user: matt
          ansible_ssh_timeout: 60
          ansible_python_interpreter: /opt/homebrew/bin/python3
    debian:
      hosts:
        miniserver:
          ansible_host: miniserver.home.lan
          ansible_user: matt
          ansible_ssh_timeout: 60
          ansible_python_interpreter: /usr/bin/python3
    arch:
      hosts:
        desktop:
          ansible_host: desktop.home.lan
          ansible_user: matt
          ansible_ssh_timeout: 60
          ansible_python_interpreter: /usr/bin/python3
          theme_color: onedark
          theme_font: firacode
          theme_style: slant
    with_login_tools:
      hosts:
        macbookair:
        desktop:
        macmini:
        miniserver:
    with_gui_tools:
      hosts:
        macbookair:
        desktop:
        macmini:
    with_browsers:
      hosts:
        macbookair:
        desktop:
        macmini:
    with_ai_tools:
      hosts:
        macbookair:
        desktop:
        macmini:
    with_nas:
      hosts:
        macbookair:
        desktop:
        macmini:
