---
all:
  children:
    macs:
      hosts:
        macbookair:
          ansible_host: macbookair.home.lan
          ansible_user: matt
          ansible_ssh_timeout: 60

    debian:
      hosts:
        debian:
          ansible_host: 143.198.177.165
          ansible_user: root

    with_login_tools:
      hosts:
        macbookair:

    with_gui_tools:
      hosts:
        macbookair:

    with_browsers:
      hosts:
        macbookair:

    with_ai_tools:
      hosts:
        macbookair:
