all:
  children:
    macs:
      hosts:
        macbookair:
          ansible_host: macbookair
          ansible_user: matt
          ansible_ssh_timeout: 60
          ansible_python_interpreter: /opt/homebrew/bin/python3
        macmini:
          ansible_host: macmini
          ansible_user: matt
          ansible_ssh_timeout: 60
          ansible_python_interpreter: /opt/homebrew/bin/python3
    debian:
      hosts:
        desktop:
          ansible_host: desktop
          ansible_user: matt
          ansible_ssh_timeout: 60
          ansible_python_interpreter: /usr/bin/python3
    arch:
      hosts:
      # archbox:
    with_login_tools:
      hosts:
        macbookair:
        desktop:
        macmini:
    with_gui_tools:
      hosts:
        macbookair:
        desktop:
        macmini:
    with_browsers:
      hosts:
        macbookair:
        desktop:
        macmini:
    with_ai_tools:
      hosts:
        macbookair:
        desktop:
        macmini:
    with_nas:
      hosts:
        macbookair:
        desktop:
        macmini:
