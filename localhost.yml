---
all:
  children:
    macs:
      hosts:
        localhost:
          ansible_connection: local
          ansible_python_interpreter: /opt/homebrew/bin/python3

    with_login_tools:
      hosts:
        localhost:

    with_gui_tools:
      hosts:
        localhost:

    with_browsers:
      hosts:
        localhost:

    with_ai_tools:
      hosts:
        localhost:
