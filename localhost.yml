---
all:
  children:
    macs:
      hosts:
        localhost:
          ansible_connection: local

    with_login_tools:
      hosts:
        localhost:

    with_gui_tools:
      hosts:
        localhost:

    with_browsers:
      hosts:
        localhost:

    with_ai_tools:
      hosts:
        localhost:
