# Apply CaskaydiaCove Nerd Font (Cascadia Code)
#
# Usage:
#   ansible-playbook themes/font_cascadia.yml
- name: Apply CaskaydiaCove Nerd Font
  hosts: all
  gather_facts: true
  tasks:
    # Install font
    - name: Install font via Homebrew (macOS)
      shell: brew install --cask font-caskaydia-cove-nerd-font
      args:
        creates: ~/Library/Fonts/CaskaydiaCoveNerdFont-Regular.ttf
      when: ansible_facts['os_family'] == "Darwin"
    - name: Install font via yay (Arch)
      shell: yay -S --noconfirm ttf-cascadia-code-nerd
      args:
        creates: /usr/share/fonts/TTF/CaskaydiaCoveNerdFont-Regular.ttf
      when: ansible_facts['os_family'] == "Archlinux"
    - name: Create fonts directory (Debian)
      file:
        path: ~/.local/share/fonts
        state: directory
      when: ansible_facts['os_family'] == "Debian"
    - name: Check if font exists (Debian)
      stat:
        path: ~/.local/share/fonts/CaskaydiaCoveNerdFont-Regular.ttf
      register: font_check
      when: ansible_facts['os_family'] == "Debian"
    - name: Download and install font (Debian)
      shell: |
        curl -fLo /tmp/CascadiaCode.tar.xz "https://github.com/ryanoasis/nerd-fonts/releases/latest/download/CascadiaCode.tar.xz"
        tar -xf /tmp/CascadiaCode.tar.xz -C ~/.local/share/fonts
        fc-cache -fv
        rm /tmp/CascadiaCode.tar.xz
      when: ansible_facts['os_family'] == "Debian" and not font_check.stat.exists
    # Update wezterm config
    - name: Update wezterm font
      ansible.builtin.replace:
        path: ~/.wezterm.lua
        regexp: 'config\.font = wezterm\.font\("[^"]+"\)'
        replace: 'config.font = wezterm.font("CaskaydiaCove Nerd Font")'
    - name: Done
      ansible.builtin.debug:
        msg: "CaskaydiaCove Nerd Font applied! Restart wezterm to see changes."
