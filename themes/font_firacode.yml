# Apply FiraCode Nerd Font
#
# Usage:
#   ansible-playbook themes/font_firacode.yml
- name: Apply FiraCode Nerd Font
  hosts: all
  gather_facts: true
  tasks:
    # Install font
    - name: Install font via Homebrew (macOS)
      shell: brew install --cask font-fira-code-nerd-font
      args:
        creates: ~/Library/Fonts/FiraCodeNerdFont-Regular.ttf
      when: ansible_facts['os_family'] == "Darwin"
    - name: Install font via yay (Arch)
      shell: yay -S --noconfirm ttf-firacode-nerd
      args:
        creates: /usr/share/fonts/TTF/FiraCodeNerdFont-Regular.ttf
      when: ansible_facts['os_family'] == "Archlinux"
    - name: Create fonts directory (Debian)
      file:
        path: ~/.local/share/fonts
        state: directory
      when: ansible_facts['os_family'] == "Debian"
    - name: Check if font exists (Debian)
      stat:
        path: ~/.local/share/fonts/FiraCodeNerdFont-Regular.ttf
      register: font_check
      when: ansible_facts['os_family'] == "Debian"
    - name: Download and install font (Debian)
      shell: |
        curl -fLo /tmp/FiraCode.tar.xz "https://github.com/ryanoasis/nerd-fonts/releases/latest/download/FiraCode.tar.xz"
        tar -xf /tmp/FiraCode.tar.xz -C ~/.local/share/fonts
        fc-cache -fv
        rm /tmp/FiraCode.tar.xz
      when: ansible_facts['os_family'] == "Debian" and not font_check.stat.exists
    # Update wezterm config
    - name: Update wezterm font
      ansible.builtin.replace:
        path: ~/.wezterm.lua
        regexp: 'config\.font = wezterm\.font\("[^"]+"\)'
        replace: 'config.font = wezterm.font("FiraCode Nerd Font")'
    - name: Done
      ansible.builtin.debug:
        msg: "FiraCode Nerd Font applied! Restart wezterm to see changes."
