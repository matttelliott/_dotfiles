# Apply SauceCodePro Nerd Font (Source Code Pro)
#
# Usage:
#   ansible-playbook themes/font_sourcecodepro.yml
- name: Apply SauceCodePro Nerd Font
  hosts: all
  gather_facts: true
  tasks:
    # Install font
    - name: Install font via Homebrew (macOS)
      shell: brew install --cask font-sauce-code-pro-nerd-font
      args:
        creates: ~/Library/Fonts/SauceCodeProNerdFont-Regular.ttf
      when: ansible_facts['os_family'] == "Darwin"
    - name: Install font via yay (Arch)
      shell: yay -S --noconfirm ttf-sourcecodepro-nerd
      args:
        creates: /usr/share/fonts/TTF/SauceCodeProNerdFont-Regular.ttf
      when: ansible_facts['os_family'] == "Archlinux"
    - name: Create fonts directory (Debian)
      file:
        path: ~/.local/share/fonts
        state: directory
      when: ansible_facts['os_family'] == "Debian"
    - name: Check if font exists (Debian)
      stat:
        path: ~/.local/share/fonts/SauceCodeProNerdFont-Regular.ttf
      register: font_check
      when: ansible_facts['os_family'] == "Debian"
    - name: Download and install font (Debian)
      shell: |
        curl -fLo /tmp/SourceCodePro.tar.xz "https://github.com/ryanoasis/nerd-fonts/releases/latest/download/SourceCodePro.tar.xz"
        tar -xf /tmp/SourceCodePro.tar.xz -C ~/.local/share/fonts
        fc-cache -fv
        rm /tmp/SourceCodePro.tar.xz
      when: ansible_facts['os_family'] == "Debian" and not font_check.stat.exists
    # Update wezterm config
    - name: Update wezterm font
      ansible.builtin.replace:
        path: ~/.wezterm.lua
        regexp: 'config\.font = wezterm\.font\("[^"]+"\)'
        replace: 'config.font = wezterm.font("SauceCodePro Nerd Font")'
    - name: Done
      ansible.builtin.debug:
        msg: "SauceCodePro Nerd Font applied! Restart wezterm to see changes."
