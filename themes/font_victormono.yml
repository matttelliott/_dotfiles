# Apply VictorMono Nerd Font
# Install: brew install --cask font-victor-mono-nerd-font
#
# Usage:
#   ansible-playbook themes/font_victormono.yml
- name: Apply VictorMono Nerd Font
  hosts: all
  gather_facts: true
  tasks:
    - name: Update wezterm font
      ansible.builtin.replace:
        path: ~/.wezterm.lua
        regexp: 'config\.font = wezterm\.font\("[^"]+"\)'
        replace: 'config.font = wezterm.font("VictorMono Nerd Font")'
    - name: Done
      ansible.builtin.debug:
        msg: |
          VictorMono Nerd Font applied!
          - Restart wezterm to see changes
          - Install font if missing: brew install --cask font-victor-mono-nerd-font
