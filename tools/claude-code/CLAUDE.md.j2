# Global Claude Code Configuration

{% if 'with_login_tools' in group_names %}
## Available CLI Tools

- `gh` - GitHub CLI for repos, PRs, issues, gists
- `doctl` - DigitalOcean CLI for droplets, kubernetes, databases
- `gcloud` - Google Cloud CLI
- `aws` - AWS CLI
- `kubectl` - Kubernetes CLI
- `helm` - Kubernetes package manager
- `kubectx` / `kubens` - Kubernetes context and namespace switcher
- `op` - 1Password CLI
{% endif %}
