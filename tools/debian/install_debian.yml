---
- name: Configure Debian system preferences
  hosts: debian
  gather_facts: false

  tasks:
    - name: Set hostname
      ansible.builtin.hostname:
        name: "{{ ansible_host }}"
      become: true
