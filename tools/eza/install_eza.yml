---
- name: Install eza
  hosts: all
  gather_facts: true

  tasks:
    - name: Install eza via Homebrew
      ansible.builtin.shell: /opt/homebrew/bin/brew install eza
      args:
        creates: /opt/homebrew/bin/eza
      when: ansible_facts['os_family'] == "Darwin"

    - name: Install eza via cargo (Debian)
      ansible.builtin.shell: ~/.cargo/bin/cargo install eza
      args:
        creates: ~/.cargo/bin/eza
      when: ansible_facts['os_family'] == "Debian"

    - name: Install eza via pacman (Arch)
      community.general.pacman:
        name: eza
        state: present
      become: true
      when: ansible_facts['os_family'] == "Archlinux"

    - name: Create zsh config directory
      ansible.builtin.file:
        path: ~/.config/zsh
        state: directory

    - name: Install eza aliases
      ansible.builtin.copy:
        src: eza.zsh
        dest: ~/.config/zsh/eza.zsh

    - name: Source eza aliases in zshrc
      ansible.builtin.lineinfile:
        path: ~/.zshrc
        line: 'source ~/.config/zsh/eza.zsh'
        create: true
