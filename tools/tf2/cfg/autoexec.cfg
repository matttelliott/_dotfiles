echo "exec autoexec"
// =============================================================================
// Autoexec - Main config entry point
// Runs automatically when TF2 starts
// =============================================================================

// Graphics settings (textures, shadows, effects, threading)
exec gfx

// Player settings (sensitivity, FOV, HUD, gameplay options)
exec settings

// Network settings (rates, interpolation)
exec network

// Custom tweaks and additional settings
exec custom
