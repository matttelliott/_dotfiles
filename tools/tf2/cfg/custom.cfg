echo "exec custom"
// =============================================================================
// Custom Tweaks
// Additional scripts and quality-of-life improvements
// =============================================================================

// Console filter - hides "Error" spam in console
exec tweaks/confilter

// Tab+net_graph - shows network stats when holding Tab (scoreboard)
// Also binds - key to toggle net_graph permanently
exec tweaks/tabgraph

// Null-cancelling movement script (uncomment to enable)
// Pressing opposite strafe keys cancels movement instead of stopping
// exec tweaks/null
